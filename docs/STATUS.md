# Фактический статус Progress City

Обновлено: **8 сентября 2026, после диагностики c63a740**. Этапы A–E и исправления F1–F4 выполнены поверх `ee06901`. Следующим отдельным запросом пользователь разрешил коммит и push исправлений в `DmitriyGrachev/ProgressiveCity`, ветка `main`. Не объявляется production-ready.

## Последний проход: диагностика

- F1: воспроизведён экспорт 40 032 205 байт с data.json больше лимита импорта. Общие пределы теперь применяются до выдачи ZIP; граничные тесты 32 MiB − 1 / ровно / + 1 байт, восстановление и предел суммы изображений проходят. В настройках есть объёмы и предупреждение от 80%.
- F2: воспроизведена потеря новой версии draft при acceptSavedCopy. Копирование блокирует все поля, ждёт загрузку/запись, защищено от повторного клика. Более новые изменения остаются dirty. Задержка копии, ошибка, повтор и поздняя картинка проверены.
- F3: реальные две вкладки воспроизвели отсутствие пути спасения после замены города. Теперь локальный черновик с исходными Blob можно выгрузить в отдельный ZIP после удаления заметки/направления или совпадения ID. Новая база не получает старый ввод. Epoch также проверяется у копии и загрузки.
- F4: два сохранения заметки давали 2 обновления и 2 пересоздания сцены; теперь 0/0. Подписки разделены по таблицам, карта читает только свои данные. Рецензент выявил смешивание epoch; добавлены транзакционные метки и тест задержки подписки.
- Чистый `npm ci` выполнен в новой `.tools/clean-install` с исходным lockfile, 249 пакетов, 0 vulnerabilities; та же Windows/Node, не другая машина. Никаких глобальных настроек или Docker.

## Сделано

- A: React/Vite/strict TypeScript, Dexie v1, старт с одной мастерской, заметки, подтверждения, отдельные улучшения, восстановление после reload.
- B: PixiJS/WebGL, карта 40 × 40, камера/pan/zoom, hit testing, предпросмотр/коллизии/границы, размещение/перенос/снятие. Три типа зданий × три этапа, имена/палитры, дороги/парк/площадь/деревья, редактируемые районы.
- C: Tiptap JSON и отдельный поисковый текст, стабильные Blob-вложения, ссылки/код/теги, библиотека и поиск. Последовательное автосохранение, flush до перехода, конфликт двух вкладок с отдельной копией. Результаты/черновики/архивирование, привычки без штрафов и с закреплёнными датами, версии правил.
- D: Независимые snapshots, исторический просмотр только для чтения, ZIP с manifest/контрольными суммами/изображениями, проверка до атомарной замены. Стартовый импорт без временного города. Epoch защищает от сохранения старой сессии после восстановления.
- E: Реальные проверки, исправления, независимое read-only ревью, синтетические скриншоты, README, ARCHITECTURE, REVIEW_REPORT.

## Фактические проверки

Все команды запускались через локальный npm: `node .tools/package/bin/npm-cli.js run <script>`.

| Скрипт | Итог |
|---|---|
| lint | PASS, exit 0 |
| typecheck | PASS, exit 0 |
| test:unit | PASS, **27/27**, 4 файла, exit 0 |
| test:e2e | PASS, **15/15**, 5 spec-файлов, 43.0 с, exit 0 |
| build | PASS, exit 0; предупреждение о chunk >500 kB не скрывалось |
| test:preview | PASS, **1/1**, собранный dist на 4174, 3.6 с, exit 0 |

Chromium 153.0.8010.12, настоящий WebGL через SwiftShader и настоящая IndexedDB в одноразовых контекстах. Основной сценарий, изображение после reload/снятия/экспорта/чистого импорта, pan/zoom/canvas, два таба, конфликт копии, инъекция ошибки записи и повтор проверены. Скриншоты в `review-evidence/` просмотрены. IAB дополнительно использовался для осмотра стартового экрана без записи тестовых данных.

Красные прогоны не скрывались: первоначально отсутствовали модули/точка входа; затем исправлены a11y-имена, смещение камеры, гонка перехода к новой заметке, выделение изображения после вставки, ложный update Tiptap при блокировке ввода и неатомарная копия после конфликта. Четыре подтверждённых P2 независимого ревью исправлены с регрессиями. В последнем полном прогоне падений нет.

## Среда и решения

- Исходно были только пользовательские документы; они сохранены. На этапе реализации Git не инициализировался, коммитов/push/PR/деплоя не было. Последующим отдельным запросом пользователь разрешил коммит и push в `DmitriyGrachev/ProgressiveCity`.
- Node **24.19.0**. `npm` в PATH отсутствовал: npm 12.0.2 из официального registry размещён в игнорируемой `.tools/package`. Все зависимости локальные, package-lock.json создан. Глобальные настройки и sandbox не менялись; Docker не использовался.
- React 19.2.8, Vite 8.2.2 + plugin-react 6.1.1, TypeScript 6.0.3, PixiJS 8.20.1, Tiptap 3.31.3, Dexie 4.4.5, Zustand 5.0.15, Zod 4.5.4, fflate 0.8.3, Vitest 5.0.0, Playwright 1.63.0. Проверены официальные документы и peer dependencies. npm install сообщил 0 vulnerabilities.
- Доменные сущности отделены от сцены; state на кадр не записывается. Бесплатное оформление не изменяет баланс. Самостоятельные векторные рисунки, без генерации концепт-арта.

## Ограничения и следующий шаг

Основной bundle 1 147.66 kB / gzip 355.10 kB; оптимизация разбиения не выполнена. Не проверены аппаратные GPU, Firefox/Safari, месяцы эксплуатации, максимальные объёмы всех сущностей и чистая установка на другой машине. Спасённый черновик и Blob хранятся в памяти вкладки: перед закрытием/перезагрузкой нужен отдельный ZIP, автоматического импорта в новый город нет. Расписание привычки текстовое; история не хранит старые редакции заметок. Нет PWA/облака/синхронизации. Неиспользуемые изображения пока сохраняются. Ограничения архивов/материалов подробно перечислены в README и ARCHITECTURE.

Для личной проверки заново запущен dev-server **http://127.0.0.1:5173/** (сессия 76987 в среде выполнения). Если сервер остановится: `npm run dev` из корня, либо `node .tools/package/bin/npm-cli.js run dev` в текущей среде. Тестовые серверы 4173/4174 после проверок остановлены автоматически. Пользовательский город не создавался и не менялся. Следующий шаг — проверить исправления на своём сценарии. Публикация исправлений разрешена отдельным запросом пользователя; PR и деплой не запрошены.

## Подготовка публикации по отдельному запросу

Проверен целевой GitHub-репозиторий `https://github.com/DmitriyGrachev/ProgressiveCity`: пустой, основная ветка `main`, доступ на запись подтверждён. Подготовлен первый коммит с исходниками, lockfile, документацией, тестами и синтетическими скриншотами. Зависимости, сборка и локальные данные исключены через `.gitignore`. PR и деплой не запрошены. Результат push проверяется сравнением локального и удалённого SHA и сообщается после выполнения.

Перед коммитом повторно выполнены lint, typecheck, test:unit (20/20), test:e2e (9/9, 30.2 с) и build — все завершились с exit 0. Предупреждение о размере bundle осталось. Проверка diff отметила только исходные двойные пробелы для Markdown-переносов в CONCEPT.md; документ сохранён без изменений.

Главные файлы передачи: `../README.md`, `ARCHITECTURE.md`, `REVIEW_REPORT.md`, `../src/storage/service.ts`, `../src/storage/validation.ts`, `../src/storage/backup.ts`, `../src/notes/save-session.ts`, `../src/city/engine.ts`, `../tests`.

## Итог публикации

По отдельному запросу пользователя проект опубликован через подключённый плагин GitHub в `DmitriyGrachev/ProgressiveCity`, ветка `main`. Основной коммит MVP: `c63a7405c28637a9bdd198667314c71e69c832ac`. Все 65 файлов, включая бинарные скриншоты, совпали с подготовленным локальным деревом Git (`72448ba7db9bded613354ba78911316f2b300e62`); `git diff HEAD origin/main` завершился без различий. Локальная `main` отслеживает `origin/main`; исходный локальный коммит сохранён в ветке `local-initial-mvp`. Обычный git push потребовал отдельного входа в Git Credential Manager, поэтому публикация выполнена через авторизованный API плагина. PR и деплой не выполнялись.
