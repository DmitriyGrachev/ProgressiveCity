# Фактический статус Progress City

Обновлено **19 сентября 2026**. Итерация **«От мастерской к своему кварталу» реализована и проверена** поверх 0cc60ad. После приёмки пользователь отдельно разрешил коммит и push в `DmitriyGrachev/ProgressiveCity`, ветка `main`. PR и деплой не запрошены.

## Сделано

- Несколько исследований/мини-проектов внутри skill/project-направления. Собственные материалы, этап 1–3, имя и следующий вопрос; общий баланс направления.
- Бесплатное начало исследования, покупка здания за **3 очка** (согласовано пользователем), выбор улучшения или расширения. Повторное размещение бесплатно.
- «Зафиксировать результат» в заметке: принудительное сохранение, готовые связи и заголовок, вывод, подтверждение, выбор развития.
- Постройка открывает материалы своего объекта; общая библиотека сохранена. Основания развития показывают расходы, правила и связанные результаты/материалы.
- Атомарная миграция IndexedDB v1 → v2, чтение ZIP v1, экспорт ZIP v2. Старые баланс, этапы, события, снимки и изображения сохранены.
- Привычки без изменений правил. Сохранились исправления F1–F4: единые пределы ZIP, сериализация копии/загрузки, восстановление локальных черновиков и отсутствие обновления сцены при автосохранении заметки.

## Проверки этой итерации

Команды реально выполнены через `node .tools/package/bin/npm-cli.js run <script>`.

| Команда | Результат |
|---|---|
| lint | PASS, exit 0 |
| typecheck | PASS, exit 0 |
| test:unit | PASS, **34/34**, 5 файлов |
| test:e2e | PASS, **20/20**, 6 файлов, около минуты |
| build | PASS; предупреждение о chunk >500 kB |
| test:preview | PASS, **2/2**, собранный dist, 9.0 с |

Chromium, настоящие IndexedDB/WebGL (SwiftShader), одноразовые контексты; dev 4173 и сборка 4174. Пройден сквозной сценарий со старым городом, вторым зданием, изображением и ZIP. Реальная нативная БД v1 тоже успешно обновилась. Проверены конкурентные расходы, откат, неизменяемые связи, конфликт исследования, задержка подписки заметок и прежние ошибки сохранения. Новые desktop/mobile-скриншоты просмотрены.

Независимый проверяющий работал только на чтение. Его замечания воспроизведены и исправлены: сброс контекста нового направления, защита имени/вопроса от второй вкладки; дополнительный тест выявил старый заголовок при быстром переходе к результату. Усилена проверка исторических этапов. Повторный просмотр не выявил новых существенных дефектов. Подробности — REVIEW_REPORT.md.

## Решения и ограничения

Спецификация/решения: QUARTER_SCOPE.md; архитектура/миграция: ARCHITECTURE.md; план G1–G5: IMPLEMENTATION_PLAN.md. Node **24.19.0**, npm отсутствует в PATH, доступен локальный npm 12.0.2. Зависимости/lockfile не менялись и заново не устанавливались. Docker, глобальные настройки и sandbox не затрагивались.

Основной bundle **1 170.28 kB**, gzip **360.54 kB**; разбиение не выполнено. Firefox/Safari, аппаратный GPU, месяцы работы и максимальные объёмы всех сущностей не проверены. Старый клиент несовместим с новой БД/ZIP v2. Нет переноса заметок между исследованиями, удаления исследования, возврата очков или облака. Несохранённые формы и спасённые черновики находятся в памяти вкладки. История не содержит прошлых редакций текста; у старых улучшений нет выдуманных ссылок на конкретные результаты.

Для запуска из корня: `node .tools/package/bin/npm-cli.js run dev` → http://127.0.0.1:5173/. При наличии npm: `npm ci`, затем `npm run dev`. Всегда сохраняйте один origin; для переноса используйте ZIP. Для личной проверки запущен Vite на 5173 (сессия 8390); тестовые серверы остановлены автоматически. Следующий шаг — личная проверка пользователем. Коммит и push разрешены отдельным запросом; результат отправки проверяется сравнением локального HEAD и удалённой main.
