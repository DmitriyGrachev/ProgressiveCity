# План реализации Progress City MVP

## Исправления по диагностике c63a740

- [x] F1. Воспроизвести несовместимость размеров, объединить лимиты ZIP/data.json, добавить предупреждение о заполнении.
- [x] F2. Проверить гонку конфликтующей копии, сериализовать копирование и ввод/загрузки, проверить отказ и повтор.
- [x] F3. Воспроизвести импорт в двух вкладках; сохранить доступ к локальному черновику и изображениям при смене epoch.
- [x] F4. Изолировать подписку сцены от материалов; измерить пересоздания при заметках и реальных изменениях карты. Каждое чтение имеет согласованный epoch.
- [x] Приёмка: чистый npm ci, lint/typecheck/unit/e2e/build, основной маршрут собранного dist, фактический отчёт. 27 unit, 15 e2e, 1 preview.

Спецификация: `MVP_SCOPE.md`, `CONCEPT.md`, `../prompts/START_CODEX.md`.
Работа в текущей папке одним исполнителем, без Docker, Git-операций и деплоя.

- [x] A. Сквозной срез. Настроить React/Vite/strict TypeScript и проверки. Начать с регрессионных тестов подтверждений, расходов и размещения. Создать типы, Dexie v1, транзакционные команды, старт города и редактор.
- [x] B. Конструктор. Изолированный PixiJS WebGL renderer; собственная векторная архитектура (3 шаблона × 3 этапа). Камера, предпросмотр, коллизии, перенос/снятие, районы, свободный декор. Проверять координаты и реальный canvas.
- [x] C. Материалы и привычки. Tiptap JSON, Blob-вложения, последовательное автосохранение, поиск, теги, связи с результатами. Привычки с неизменяемым локальным ключом дня; версии правил и архивирование без удаления данных.
- [x] D. История и резервирование. Глубокие снимки планировки, режим чтения, ZIP manifest/data/attachments. Ограниченная распаковка, строгая валидация связей и атомарная замена только после подтверждения.
- [x] E. Приёмка. Реальные lint/typecheck/unit/e2e/build; браузерный сценарий из MVP_SCOPE, две вкладки, ошибки хранения, импорт, скриншоты. README, ARCHITECTURE, STATUS, REVIEW_REPORT.

Модули: `src/domain` — типы/правила/геометрия; `src/storage` — БД/команды/архив; `src/city` — рисунок и камера; `src/notes` — редактор/сохранение; `src/features` — панели направлений, активностей, истории и настроек; `src/app` — композиция и временное состояние Zustand. Vitest проверяет реальные команды с fake-indexeddb; Playwright — настоящую IndexedDB и WebGL в одноразовых контекстах.

Решения: приложение занимает экран; светлая нейтральная оболочка, зелёная карта, контекстная правая панель. Сгенерированный концепт-арт не используется по заданию. Свободное оформление никогда не меняет баланс. IndexedDB является источником данных, Zustand хранит только состояние интерфейса. Изменения заметок сохраняются последовательно и принудительно завершаются перед навигацией.
