# Удобная планировка — 19.09.2026

Спецификация: `PLANNING_SCOPE.md`. Работа одним исполнителем в текущей папке; после проверки — разрешённые коммит и push main.

- [x] J1. `domain/roads.ts`, `storage/layout.ts`: связный маршрут, атомарные изменения зданий/районов, история 100 команд с проверкой epoch и конфликтов. 13 unit-регрессий: целый жест, коллизия/граница, отказ события, повтор, две вкладки, сохранность прогресса и платных связей.
- [x] J2. `city/engine.ts`, `BuildPanel`, `CityCanvas`: предпросмотр пути, захват/отмена указателя, серийный декор с новыми ID, одноразовый перенос и учебное здание, состояние сохранения. Проверено настоящим canvas, включая отказ записи и повтор жеста.
- [x] J3. Кнопки и безопасные сочетания клавиш; сброс после импорта; браузерная приёмка, сохранение после reload, текстовый Ctrl+Z, исторический вид, desktop/mobile снимки. 3 новых сценария также прошли на собранном dist.
- [x] J4. Read-only ревью; реальные lint/typecheck/unit/e2e/build/preview; STATUS, REVIEW_REPORT, README, ARCHITECTURE. Проверки PASS: 63 unit, 28 e2e, 9 preview; lint/typecheck/build PASS. Далее разрешённые коммит/push и сверка HEAD.

Контракты: `LayoutService` хранит только before/after затронутой геометрии в памяти; запись и событие проходят в одной Dexie-транзакции. UI получает стабильный subscribe/getSnapshot для кнопок. Renderer сообщает готовый маршрут, не пишет в базу на движениях. Существенные края проверки: быстрый/прерванный жест, пересечение существующей дороги, ошибка сохранения, импорт из соседней вкладки, отмена в редакторе.

---

# Полный размер ZIP после миграции — 19.09.2026

- [x] I1. Воспроизвести на настоящих fflate/Zod/командах переход допустимого v1 через 64 MiB общего ZIP; увидеть RED. 3 красных / 4 зелёных; принятый старый ZIP на 1024 байта меньше предела не экспортировался после миграции.
- [x] I2. Общий синхронный допуск полного STORE-архива (JSON, Blob, manifest, ZIP-заголовки) в import/replace/upgrade/export; сохранить v1 при отказе и объяснять конкретный лимит.
- [x] I3. Проверить −1/0/+1 байт, оба пути миграции, v1 recovery и v2 round-trip; сохранить проверки 32 MiB и проверить экран в Chromium. Целевые проверки и decode синтетического PNG прошли; read-only reviewer не нашёл существенных дефектов.
- [x] I4. Последовательно выполнить lint, typecheck, unit, e2e, build, preview; обновить отчёт/статус/архитектуру. Все проверки PASS: 50 unit, 24 e2e, 6 preview. Публикация в main разрешена постоянным указанием пользователя; после отправки сверяются локальный и удалённый HEAD.

---

# Исправление границы миграции — 19.09.2026

- [x] H1. Воспроизвести через настоящие ZIP/Zod/IndexedDB переход экспортируемого v1 за предел 32 MiB после миграции. До исправления: 3 красных, 4 зелёных пограничных теста.
- [x] H2. Проверять итоговый UTF-8 JSON (включая метаданные изображений) до замены/записи миграции; сохранить старую схему, записи, epoch и Blob при отказе. 7/7 целевых проверок прошли.
- [x] H3. Дать скачать неизменённый ZIP v1 из экрана отказа обновления; проверить настоящий Chromium и повторный запуск. Проверены текст, изображения, SHA-256 и сохранение v1/epoch.
- [x] H4. Запустить полные проверки и обновить STATUS / REVIEW_REPORT / описание восстановления. lint/typecheck/build PASS; 42 unit, 22 e2e, 4 preview. Снимки реального экрана отказа на desktop/mobile просмотрены.

---

# Итерация «От мастерской к своему кварталу» — 19.09.2026

Спецификация: `docs/QUARTER_SCOPE.md`. Реализация в текущей папке, без коммита и публикации.

- [x] G1. `domain/model`, `storage/db`, миграция и валидация: добавить учебные объекты, перенести старые связи/этапы; тест реальной базы v1 и старого ZIP с изображением.
- [x] G2. `storage/service`, `storage/backup`: независимые этапы, общий баланс, покупка строительства, неизменяемые связи; тесты конкурентных команд, нехватки очков, отката транзакции, повреждённых архивов и v2 round-trip.
- [x] G3. `TrackPanel`, `ActivityForm`, `NoteEditor`, `BuildPanel`, `Library`, `Settings`, `ui`: исследования, результат из заметки, выбор развития, следующий вопрос и доступ через постройку. Сохранить защиту черновиков/epoch.
- [x] G4. `CityCanvas`, `engine`, `use-city-data`: собственные этапы/выбор объектов без перерисовки при автосохранении; настоящий браузерный сценарий с двумя зданиями, изображением, ZIP и перезапуском.
- [x] G5. Реально выполнить lint, typecheck, test:unit, test:e2e, build, test:preview; независимое чтение изменений; обновить STATUS, REVIEW_REPORT, ARCHITECTURE, README и снимки.

Особое внимание: город без здания; архивное направление; повтор покупки двумя вкладками; поддельные связи/этапы при импорте; незавершённый черновик при переходе к результату.


Итог: lint/typecheck/build — PASS; 34 unit, 20 e2e, 2 preview. Независимое read-only ревью и регрессии его замечаний завершены. Состояние и ограничения — STATUS.md / REVIEW_REPORT.md.

---

# План реализации Progress City MVP

## Исправления по диагностике c63a740

- [x] F1. Воспроизвести несовместимость размеров, объединить лимиты ZIP/data.json, добавить предупреждение о заполнении.
- [x] F2. Проверить гонку конфликтующей копии, сериализовать копирование и ввод/загрузки, проверить отказ и повтор.
- [x] F3. Воспроизвести импорт в двух вкладках; сохранить доступ к локальному черновику и изображениям при смене epoch.
- [x] F4. Изолировать подписку сцены от материалов; измерить пересоздания при заметках и реальных изменениях карты. Каждое чтение имеет согласованный epoch.
- [x] Приёмка: чистый npm ci, lint/typecheck/unit/e2e/build, основной маршрут собранного dist, фактический отчёт. 27 unit, 15 e2e, 1 preview.

Спецификация: `MVP_SCOPE.md`, `CONCEPT.md`, `../prompts/START_CODEX.md`.
Работа в текущей папке одним исполнителем, без Docker, Git-операций и деплоя.

- [x] A. Сквозной срез. Настроить React/Vite/strict TypeScript и проверки. Начать с регрессионных тестов подтверждений, расходов и размещения. Создать типы, Dexie v1, транзакционные команды, старт города и редактор.
- [x] B. Конструктор. Изолированный PixiJS WebGL renderer; собственная векторная архитектура (3 шаблона × 3 этапа). Камера, предпросмотр, коллизии, перенос/снятие, районы, свободный декор. Проверять координаты и реальный canvas.
- [x] C. Материалы и привычки. Tiptap JSON, Blob-вложения, последовательное автосохранение, поиск, теги, связи с результатами. Привычки с неизменяемым локальным ключом дня; версии правил и архивирование без удаления данных.
- [x] D. История и резервирование. Глубокие снимки планировки, режим чтения, ZIP manifest/data/attachments. Ограниченная распаковка, строгая валидация связей и атомарная замена только после подтверждения.
- [x] E. Приёмка. Реальные lint/typecheck/unit/e2e/build; браузерный сценарий из MVP_SCOPE, две вкладки, ошибки хранения, импорт, скриншоты. README, ARCHITECTURE, STATUS, REVIEW_REPORT.

Модули: `src/domain` — типы/правила/геометрия; `src/storage` — БД/команды/архив; `src/city` — рисунок и камера; `src/notes` — редактор/сохранение; `src/features` — панели направлений, активностей, истории и настроек; `src/app` — композиция и временное состояние Zustand. Vitest проверяет реальные команды с fake-indexeddb; Playwright — настоящую IndexedDB и WebGL в одноразовых контекстах.

Решения: приложение занимает экран; светлая нейтральная оболочка, зелёная карта, контекстная правая панель. Сгенерированный концепт-арт не используется по заданию. Свободное оформление никогда не меняет баланс. IndexedDB является источником данных, Zustand хранит только состояние интерфейса. Изменения заметок сохраняются последовательно и принудительно завершаются перед навигацией.
